import heatmapGenerator from 'dummy/utils/heatmap-generator';
import { module, test } from 'qunit';

module('Unit | Utility | heatmapGenerator', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = heatmapGenerator();
    assert.ok(result);
  });
});
