  
<li class="nav-item ml-3">
  {{#power-select
  options=metrics
  triggerClass="metric-selector"
  selected=choice
  onchange=(action "selectMetric")
  placeholder="Select metric..."
  searchEnabled=false
  matchTriggerWidth=false
  as |metric|}}
    {{capitalize metric.name}}
    <HelpTooltipRight @title={{metric.description}}/>
  {{/power-select}}
</li>